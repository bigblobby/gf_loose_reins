{# TODO add stars next to require fields #}

{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.email) }}
    {{ form_row(form.message) }}
    {{ form_row(form.subscribe) }}
    {{ form_row(form.submit) }}
{{ form_end(form) }}